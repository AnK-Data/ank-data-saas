# ANK Data — Dependências Python
# Usadas por scripts auxiliares de processamento de dados (pasta scripts/)
#
# Instalação:
#   pip install -r requirements.txt

# Manipulação de planilhas Excel (.xlsx, .xls)
pandas>=2.0.0
openpyxl>=3.1.0
xlrd>=2.0.1

# Utilitários de ambiente
python-dotenv>=1.0.0

# Nota: unicodedata e re são módulos built-in do Python — não precisam ser instalados.
