# ANK Data — Dependências Python
# Scripts de processamento e padronização de dados (pasta scripts/)
#
# Instalação:
#   pip install -r requirements.txt

# Manipulação de planilhas Excel (.xlsx, .xls)
pandas>=2.0.0
openpyxl>=3.1.0
xlrd>=2.0.1

# Normalização de strings (nomes, endereços)
# unicodedata e re são built-in do Python — não precisam de instalação

# Utilitários
python-dotenv>=1.0.0    # Leitura de variáveis de ambiente (.env)
